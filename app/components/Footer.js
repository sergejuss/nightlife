import React, { PropTypes } from 'react';
import styles from '../styles';

const Footer = (props) => {
  return (
    <div className="col-sm-12 text-center" style={styles.spaceTop}>
      using Yelp API
    </div>
  )
}

export default Footer
